{
  "meta": {
    "schema_version": "1.1.0",
    "awf_version": "4.0.2"
  },
  "project": {
    "name": "EV-Plus",
    "type": "Android App",
    "status": "In Progress"
  },
  "tech_stack": {
    "frontend": {
      "language": "Kotlin",
      "ui_framework": "Jetpack Compose",
      "image_loader": "Coil Compose 2.6.0",
      "auth_library": "AndroidX Credentials / Google ID"
    },
    "backend": {
      "service": "Firebase (Firestore + Auth)",
      "telemetry_source": "EVCS Public API (REST, direct VinFast CDN)"
    },
    "database": {
      "cloud": "Cloud Firestore (users/{userId}/userdata/favorites)",
      "local": "Local-First SharedPreferences / JSON Cache"
    }
  },
  "database_schema": {
    "summary": "Cloud Firestore stores user favorites under /users/{userId}/userdata/favorites as a map of station IDs to timestamps.",
    "tables": [
      {
        "name": "favorites",
        "path": "users/{userId}/userdata/favorites",
        "fields": [
          "favorites (Map<String, Long>)",
          "updated_at (Timestamp/Long)"
        ]
      }
    ],
    "relationships_explained": "Each user owns exactly one favorites document at /users/{userId}/userdata/favorites."
  },
  "features": [
    "Native Station Detail Bottom Sheet with VinFast S3 Photo Carousel and Lightbox Zoom",
    "Local-First Firestore Favorites Sync with Anonymous & 1-Tap Google Sign-In",
    "Direct CDN Media URL Decoding (Bypassing Cloudflare HTTP 403)"
  ],
  "knowledge_items": {
    "patterns": [
      "VinFast CDN media double base64 URL decoding to bypass evcs.vn 403",
      "Firestore Map structure favorites.[stationId] for atomic add/delete"
    ],
    "gotchas": [
      "evcs.vn /media endpoint blocks requests with Cloudflare 403; decode double base64 to cpo-prod-s3.vinfastauto.com direct URL instead",
      "FieldValue.arrayRemove requires exact byte match; use Map updates instead"
    ]
  }
}
